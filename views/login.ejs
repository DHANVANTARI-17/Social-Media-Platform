<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  
</head>
<body>
    <% if (error_msg.length > 0) { %>
        <div id="error_msg_id" class="alert alert-danger  text-red-500 absolute left-1/2 transform -translate-x-1/2  p-4 rounded-md">
            <%= error_msg %>
        </div>
    <% } %>
    <div class="w-full min-h-screen bg-zinc-900 text-white flex items-center justify-center">
        
        <div class=" bg-transparent py-2 rounded-lg shadow-md w-full max-w-md border-2 border-zinc-800">
            <form action="/login" method="post" class="space-y-4 flex flex-col items-center">
                <h3 class="text-3xl mb-3 text-center underline">Login</h3>
                <input class="px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-800 w-3/4" type="email" placeholder="email" name="email">
                <input class="px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-800 w-3/4" type="password" placeholder="password" name="password">
                <input class="px-4 py-2 rounded-md bg-blue-500 text-lg mt-2" type="submit" value="Submit">
                <a class="text-blue-500" href="/">Create account?</a>
            </form>
        </div>
    </div>
    <script src="\js\Flash.js"></script>
</body>
</html>